module Main
import dotplugin::Display;
import Mies;
import IO;
import lang::dot::Dot;

public void main() {
    // dotDisplay(|project://dotplugin/src/a.dot|);
    // writeFile(|file:///tmp/aap.dot|, toString(g));
    dotDisplay(g);    
}
